type Lead {
  id: ID!
  name: String!
  email: String!
  mobile: String!
  postcode: String!
  services: [String!]!
}

type Query {
  leads: [Lead!]!
  lead(id: ID!): Lead
}

type Mutation {
  register(name: String!, email: String!, mobile: String!, services: [String!]!, postcode: String!): Lead!
}